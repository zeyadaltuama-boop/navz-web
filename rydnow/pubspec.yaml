name: navz
description: NAVZ mobile application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🌍 Localization (built into Flutter)
  flutter_localizations:
    sdk: flutter

  # 🌐 Languages / formatting
  intl: ^0.19.0

  # 🧠 State management
  provider: ^6.1.2

  # 📍 Google Places (real autocomplete)
  google_place: ^0.4.7

  # 🌐 HTTP (compatible with google_place)
  http: ^0.13.6

  # 🔗 External links
  url_launcher: ^6.3.1

  # 📤 Share journey
  share_plus: ^12.0.1

  # 🍎 iOS icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
